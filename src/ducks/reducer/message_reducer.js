const initialState = {
    messageArray: [],
};

// *** ACTION TYPES *** //


// *** REDUCER *** //

export default function reducer(state = initialState, action) {
    switch(action.type) {
        default: 
            return {state};
    };
}

// *** ACTION CREATORS *** //